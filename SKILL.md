---
name: hyperframesshorts
description: 한국어 유튜브 쇼츠를 주제 선정부터 AI 동영상·이미지·기존 목소리·자막·전환·썸네일·최종 MP4까지 제작하거나 수정한다. Omni Flash 360p 생성과 Hyperframes 편집, 영상 제목별 폴더 저장이 필요한 요청에 사용한다.
---

# Hyperframes Shorts 제작

사용자가 주제와 제작 의도를 정했다면 실제 재생 가능한 영상과 썸네일까지 완성한다. 기획만 요청했다면 기획까지만 한다. 기존 작업을 수정할 때는 해당 수정만 수행한다. 새 지시가 아래 기본값보다 우선한다.

## 제작 기본값

- **Flow 브라우저: Aside 우선, 백그라운드 작업.** 기존 Aside 로그인·프로젝트를 재사용한다. 현재 도구가 Aside를 제어할 수 있는지와 화면을 앞으로 가져오지 않고 입력·다운로드할 수 있는지를 별도로 확인한다. 앱을 읽을 수 있다는 사실만으로 백그라운드 조작 가능이라고 판단하지 않는다. 구체적인 절차는 [Aside와 백그라운드 작업](references/production.md#aside와-백그라운드-작업)을 따른다.

- **동영상 생성: Omni Flash, 360p.** 화면이나 도구에서 실제 모델명과 해상도를 확인한다. Google Flow라는 서비스 이름만 보고 Omni를 썼다고 판단하지 않는다. 해당 서비스에 Omni가 없으면 접근 가능한 Omni 제공 서비스/연결 도구를 찾는다. 모두 없으면 로그인·연결이 필요한 지점만 설명하고 이미지·대본 등 독립 작업을 진행한다. Veo 등 다른 모델로 조용히 대체하지 않는다.
- **길이:** 총 45초를 기본으로 하되 사용자 지정 길이를 우선한다. 장면별 생성 초수는 내레이션·편집 흐름과 서비스가 지원하는 옵션에 맞춰 직접 정한다. 장면 길이를 매번 묻지 않는다.
- **영상과 이미지 혼합:** 동작은 실제 생성된 동영상으로, 구조·비교는 설명 이미지로 보여 준다. 이미지만 움직인 슬라이드쇼를 동영상 생성 완료라고 부르지 않는다.
- **원리·구조 설명:** 설명 구간에는 내용에 맞는 2D·3D 도해, 그래프, 비교표 또는 시뮬레이션을 실제로 제작해 영상에 넣는다. 구조·힘·움직임·변화의 관계를 내레이션과 맞춰 단계적으로 보여 준다. 표현 방식은 직접 선택하며 모든 형식을 한 영상에 넣을 필요는 없다. 수치와 모델의 한계를 포함한 구체적인 제작 기준은 [원리·구조 시각화](references/production.md#원리구조-시각화)를 따른다.
- **장면·구도 다양성:** 비슷한 배경의 같은 옆모습을 반복하지 않는다. 대본의 의미에 맞춰 환경을 보여 주는 넓은 장면, 전신 동작, 얼굴·부위 근접, 위·아래·정면·사선 각도, 설명용 구조·비교 장면을 섞는다. 인접 장면은 거리·각도·행동·배경 중 두 가지 이상을 달리하는 것을 기본으로 하되, 동일 동작을 이어 보여 줄 때는 연속성을 우선한다. 같은 원본을 확대하거나 좌우 반전한 것만으로 새로운 구도로 계산하지 않는다. 생성 전에 장면별 구도를 정하고, 생성 결과가 비슷하면 다른 구도로 수정·재생성한다. 구도가 바뀌어도 대상의 외형과 사실 관계는 유지한다.
- **배경음악:** 별도 지정이 없는 모든 쇼츠는 스킬 폴더의 `assets/music/2026-09-01_12-02-40_create-a-bright-warm-playful-background-.wav`를 기본으로 사용한다. 기존 동물·곤충·생물·신기한 현상류에 사용하던 밝고 따뜻한 음악이며 사용자가 공개 포함을 승인했다. 스킬 위치 기준으로 경로를 해석해 영상별 assets에 복사한다. 음악은 정상 속도로 영상 길이만큼 반복하고 -28 LUFS를 출발점으로 내레이션보다 작게 믹싱한다. 첫 0.4초와 마지막 2초는 부드럽게 페이드하며 짧은 영상에서는 조정한다. 음성의 1.2배속을 음악에 적용하지 않는다. 사용자가 다른 음악 또는 무음악을 지정하면 그 지시를 우선한다. 설치 파일이 누락됐으면 저장소에서 복구하고 임의 음악으로 대체하지 않는다.
- **목소리:** 먼저 `~/.config/hyperframesshorts/voice.json`의 선택을 확인하고 그대로 사용한다. 미설정이면 사용자에게 1. 로컬 무료(수익화 가능, 목소리 권리 준수, 컴퓨터 자원 소모), 2. ElevenLabs 무료 API(수익화 불가, 공개 시 출처 표시), 3. ElevenLabs 구독(유료, 구독 중 생성물 상업 이용 가능) 중 하나를 묻는다. 로컬일 때만 VoiceStudio/VoxCPM2와 공유 목소리를 준비한다. ElevenLabs 선택 시 VoiceStudio를 설치·실행하거나 로컬 모델을 다운로드하지 않는다. API 연결 실패 시 다른 엔진으로 전환하지 않는다. 키는 로컬 환경변수로 받고 채팅에 요청하지 않는다. [음성 설정·생성](references/voice.md)을 따른다.
- **음성 속도: 생성 원본 대비 1.2배속.** 원본에 정확히 한 번 적용하고 최종 가속 음성으로 자막과 장면을 정렬한다. 배경음악은 정상 속도를 유지한다.
- **자막:** 기존 영상이 있으면 분석해 맞춘다. 기본은 Wanted Sans ExtraBold이며 없으면 사용 가능한 Pretendard Bold, 흰색 글자·검은 테두리, 하단 짧은 1–2줄, 문장보다 짧은 의미 단위로 교체한다.
- **화면 문구:** 제작·검토 상태나 불필요한 `AI 생성` 문구를 영상에 덧붙이지 않는다. 설명용 도식의 가정·한계는 제작정보에 남기고, 실제 측정 화면으로 오해할 수 있는 경우에는 필요한 범위에서 모식도임을 알린다. 서비스 원본의 공식 워터마크는 유지한다.
- **전환:** 짧은 줌·블러(약 0.3초), 이미지의 완만한 확대. 핵심 동작과 첫 화면을 긴 전환으로 가리지 않는다.
- **썸네일:** 별도 세로 9:16 이미지. 주제를 바로 읽을 수 있는 큰 한국어 문구, 강한 대비, 핵심 피사체. 모바일 크기로 글자 오류와 잘림을 확인한다.
- **출력:** 세로 9:16 MP4, 기본 1080×1920/30fps/H.264/AAC. 360p 생성 원본을 확대 출력한 사실을 제작 기록에 남긴다. 원본을 1080p로 생성했다고 표현하지 않는다.

## 저장과 재개

기본 루트는 `~/Projects/Youtube`이다. 현재 사용자의 홈 경로를 기준으로 해석한다. 사용자가 다른 루트를 지정하면 그것을 따른다.

```text
~/Projects/Youtube/영상 제목/
  영상 제목.mp4
  썸네일.png
  대본.txt
  자막.srt
  유튜브_제목과설명.txt
  제작정보.md
  index.html
  assets/                 # 생성 동영상·이미지·음성 원본
```

파일명에 쓸 수 없는 `/`, `\`, 제어 문자는 `_`로 바꾸고 제목의 나머지는 유지한다. 동명 폴더에 기존 작업이 있으면 의도한 수정인지 확인하고 재개한다. 별도 신작이면 버전 접미사를 사용한다. 도구가 임시 폴더에 결과물을 저장하면 원본을 유지한 채 위 폴더로 복사한다. 공개가 명시적으로 승인된 `assets/voice/`·`assets/music/` 외의 개인 영상·이미지·음성은 GitHub에 넣지 않는다.

## 처음 사용하는 환경

이 스킬은 [aihubos/hyperframesshorts](https://github.com/aihubos/hyperframesshorts)의 사용자 제작 워크플로다. HeyGen의 공식 Hyperframes 엔진과 구분되며, 설치 폴더와 호출 이름도 `hyperframesshorts`다.

- 다른 사용자 제작 스킬은 필수 의존성이 아니다. 이 패키지의 `references/production.md`에 기본 편집 규칙이 포함된다. 다른 스킬이 설치되어 있다고 가정하거나 자동으로 추가 설치하지 않는다.
- 엔진이 설치된 사용자는 저장소의 `python3 install.py --setup`로 스킬과 선택한 음성 환경을 준비한다. AI는 먼저 음성 방식을 물어본 뒤 `--voice-provider local|elevenlabs-free|elevenlabs-paid` 중 선택값을 전달한다. 스킬만 필요하면 `python3 install.py`를 사용한다. Windows 10/11 x64에서도 `py -3 install.py --setup`로 동일한 음성 설치 흐름을 사용한다. Windows 실기 설치·렌더링 검증은 별도로 필요하다. 기본 대상은 Codex이며, 다른 에이전트는 `--skills-dir`로 설치 경로를 지정한다. 기존 동명 스킬은 백업한다. 설치된 스킬 폴더에서는 설치 프로그램이 없으므로 업데이트는 원래 Git 저장소에서 실행한다.
- 기존 프로젝트의 Hyperframes CLI를 재사용한다. `hyperframes --version` 또는 프로젝트의 `node_modules/.bin/hyperframes --version`으로 확인한다. 엔진이 없는 사용자만 저장소 README의 `--setup-runtime` 절차를 선택한다. 기존 엔진을 자동 업데이트하지 않는다.
- 설치한 `SKILL.md`를 직접 읽으면 현재 요청에 적용할 수 있다. 다음 세션의 자동 검색은 앱을 재시작한 뒤 확인한다. 파일 설치와 스킬 목록 노출은 구분한다.
- 동영상·이미지 생성 도구, 브라우저/컴퓨터 사용 도구, 선택한 음성 환경(로컬 또는 ElevenLabs API)은 별도로 필요하다. VoiceStudio 설치·모델 다운로드·개인 목소리 등록은 [음성 설정 안내](references/voice.md)를 따른다. GPT 이미지 생성 도구가 제공되면 설명 이미지와 썸네일에 우선 사용한다. 도구가 없다면 사용 가능한 생성 서비스 또는 사용자 제공 원본으로 진행하며, 없는 기능을 있다고 보고하지 않는다.
- 이 스킬은 엔진이나 모델, 유료 계정, 폰트를 포함하지 않는다. 공개 승인된 목소리 참조와 기본 배경음악은 포함한다. 로그인은 각자의 계정을 사용하고 키·쿠키를 대화나 공개 저장소로 공유하지 않는다. 필요한 사용자 파일이 없으면 요청하고 독립 작업은 계속한다.

## 실행 순서

1. 기존 대본·장면·원본·완성본과 사용자 지시부터 확인한다. 명확한 요청에 중복 질문하지 않는다.
2. 사실 기반 영상은 신뢰할 수 있는 원문을 확인한다. 첫 몇 초에 눈에 보이는 이상한 점이나 사실에 근거한 반전을 제시한다. AI 설명 이미지를 실제 연구 촬영으로 제시하지 않는다.
3. 대본과 장면 길이를 잡고 Omni Flash 360p 동영상, 설명 이미지, 썸네일을 생성한다. Flow는 Aside의 현재 로그인된 페이지와 제공된 컴퓨터 사용 도구를 우선 활용하고 백그라운드 작업 규칙을 따른다. 요청된 모델·옵션을 생성 직전에 확인한다.
4. 승인한 목소리로 내레이션을 생성하고 원본 대비 1.2배속을 적용한 뒤 단어별 음성 인식/강제 정렬 및 무음 구간을 이용해 실제 발화 시점에 자막과 장면을 배치한다. 글자 수 비율만으로 자막 시간을 배분하지 않는다. 정확한 정렬 도구가 없으면 쉼·문구를 기준으로 직접 확인·조정하고 검수 범위를 제작정보에 기록한다.
5. [편집·저장 참고](references/production.md)를 읽고 기존 Hyperframes 구성을 재사용하여 렌더한다. 최종 MP4는 생성 원본과 별도로 저장한다.
6. 파일 길이·해상도·영상/음성 트랙, 전체 디코딩, 핵심 장면의 자막 잘림·전환·빈 화면을 확인한다. 가능하면 실제 브라우저 재생도 확인한다. 생성/출력/재생/업로드는 서로 다른 상태로 보고한다.
7. 완성 MP4와 썸네일을 절대 경로 링크 또는 미리보기로 전달하고, 아래 최종 답변 규칙을 적용한다. 게시 요청이 없으면 업로드하지 않는다.

## 최종 답변: 제목과 설명을 채팅창에 제공

새 영상 또는 수정한 완성 영상을 전달할 때는 사용자가 따로 요청하지 않아도 **같은 최종 채팅 답변에 실제 유튜브 제목과 설명의 전체 내용을 반드시 포함**한다. `제목`과 `설명`을 각각 별도의 `text` 코드 블록으로 제공해 바로 복사할 수 있게 한다. 코드 블록 안에는 게시할 내용만 넣고 안내 문구나 미완성 자리표시자를 넣지 않는다.

같은 내용을 영상 폴더의 `유튜브_제목과설명.txt`에도 저장한다. 파일 저장, 파일 링크, 미리보기 또는 "제목·설명도 준비했습니다"라는 안내만으로 채팅 출력 요건을 대신하지 않는다. 영상 내용을 수정했다면 제목·설명도 최종 영상과 일치하는지 확인한다. 최종 답변을 보내기 전에 **영상·썸네일 링크와 제목·설명 코드 블록 두 개**가 모두 있는지 확인한다. 사용자가 특정 항목만 요청하거나 별도 출력 형식을 지정한 경우에는 그 범위를 따른다.

멀티에이전트 테스트·보안·해시 작업이나 멀티에이전트 리뷰를 요구하지 않는다. 현재 요청에 필요한 제작·출력 확인만 직접 수행한다.
